class Date 
{
  FileIO weather = new FileIO();
    // Gets the strings in the table of years and month
   String [] year = weather.dataTable.getStringColumn("Year");
   String [] month = weather.dataTable.getStringColumn("Month");
   
   
}
